

<%= form_with(model: [@question, @answer], local: true) do |form| %>
    <div class="answer      -container">
    <p class="title-lg"><%= form.label :content, 'Текст відповіді' %></p>
    <p><%= form.text_area :content, rows: 5 , class: 'form-text-area' %></p>
    </div>
    <p class="mt"> <%= form.submit 'Зберегти', class: 'button button button-main' %></p>
<% end %> 





